<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/dp_10"
        android:text="@string/text_masai_school" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/dp_10"
        android:text="@string/text_hello_world" />
</LinearLayout>

    <!-- padding - Adds some white space inside the view in all the 4 directions i.e top, bottom, start and end-->
    <!-- text - the content to be displayed inside the view -->
    <!-- @string/name - gets the text from the strings.xml folder, never hardcode the values in the files -->
    <!-- orientation - how the content should be aligned, either horizontal or vertical -->